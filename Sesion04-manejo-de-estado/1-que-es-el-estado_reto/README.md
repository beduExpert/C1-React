## Qué es el estado (reto)

- Modifica el componente `App` para que tenga un estado dónde se guardará un
  arreglo de países.

- Crea un componente funcional dónde reciba via props el nombre del país e imágen
